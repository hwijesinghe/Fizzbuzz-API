npm init -y     // (-y optional if -y automatically generate package.json file with default information)
npm i nodemon   // This is for server to run continuousely - live server running 
npm i express   // Server request and response to handle
npm i dotenv    // dotenv for keep creadentials and port information hidden from code

npm install -D jest // Testing packages 
npm install -D supertest // Testing packages 

npm i http-errors // This will help to find http erroes in express application


//"test": "echo \"Error: no test specified\" && exit 1",

Endpoints

//http://localhost:8000/
//http://localhost:8000/fizzbuzz/58
//http://localhost:8000/fizz/10
//http://localhost:8000/buzz/15

